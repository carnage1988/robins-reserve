# Runtime secrets

Do not commit credentials to this directory.

The deployed service expects:

- `.env` in the project root
- `secrets/google-service-account.json`

Copy these securely to the deployment server before starting the container.
